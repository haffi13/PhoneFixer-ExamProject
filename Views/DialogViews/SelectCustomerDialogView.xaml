<Window x:Class="Views.DialogViews.SelectCustomerDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Views.DialogViews"
        mc:Ignorable="d"
        Title="SelectCustomerDialogView" Height="300" Width="300">
    <Grid>
        <DataGrid
            Grid.Column="1"
            Grid.ColumnSpan="7"
            Grid.Row="1"
            Grid.RowSpan="5"
            Margin="10"
            AutoGenerateColumns="True"
            IsReadOnly="True"
            ItemsSource="{Binding Customers, UpdateSourceTrigger=PropertyChanged}"
            SelectedItem="{Binding SelectedCustomer}"
            />
        <Button Content="sdfsdfsdf"
                Command="{Binding ClickBtn}"></Button>
    </Grid>
</Window>
